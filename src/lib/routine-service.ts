import { collection, doc, onSnapshot, orderBy, query, serverTimestamp, setDoc, type Unsubscribe } from "firebase/firestore";
import { db } from "./firebase";
import type { Routine, RoutineCategory, RoutineLog, RoutineStatus } from "../types/tracker";
const routinesRef = (uid: string) => collection(db, "users", uid, "routines");
const logsRef = (uid: string) => collection(db, "users", uid, "routine_logs");
const asRoutine = (id: string, value: any): Routine => ({ id, name: String(value.name || "Routine"), category: value.category || "personal", priority: ["critical", "high", "normal"].includes(value.priority) ? value.priority : "normal", time: value.time || "09:00", durationMinutes: Number(value.durationMinutes || 15), windowMinutes: Number(value.windowMinutes || 15), repeatDays: Array.isArray(value.repeatDays) ? value.repeatDays : [0,1,2,3,4,5,6], reminderMinutes: Number(value.reminderMinutes || 5), sessionBehavior: value.sessionBehavior === "pause" ? "pause" : "warn", strict: Boolean(value.strict), active: value.active !== false, createdAt: value.createdAt || null, updatedAt: value.updatedAt || null });
export const subscribeToRoutines = (uid: string, callback: (items: Routine[]) => void): Unsubscribe => onSnapshot(query(routinesRef(uid), orderBy("time")), snap => callback(snap.docs.map(item => asRoutine(item.id, item.data()))));
export const subscribeToRoutineLogs = (uid: string, callback: (items: RoutineLog[]) => void): Unsubscribe => onSnapshot(logsRef(uid), snap => callback(snap.docs.map(item => ({ id: item.id, ...item.data() } as RoutineLog))));
export const saveRoutine = (uid: string, input: Omit<Routine, "id" | "createdAt" | "updatedAt">, id?: string) => setDoc(id ? doc(routinesRef(uid), id) : doc(routinesRef(uid)), { ...input, createdAt: id ? undefined : serverTimestamp(), updatedAt: serverTimestamp() }, { merge: true });
export const logRoutine = (uid: string, routineId: string, dateString: string, status: RoutineStatus, patch: Partial<RoutineLog> = {}) => setDoc(doc(logsRef(uid), `${routineId}-${dateString}`), { routineId, dateString, status, completedAt: status === "completed" ? serverTimestamp() : null, ...patch }, { merge: true });
